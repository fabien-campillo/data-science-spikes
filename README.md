# README
This repository contains a Jupyter Book called **Data Science & Spikes**  designed to help you get started in data science by exploring electrophysiological data, especially spike train recordings, in a practical and accessible way.

[![Jupyter Book Badge](https://jupyterbook.org/badge.svg)](https://fabien-campillo.github.io/data-science-spikes/)
[![Python Version](https://img.shields.io/badge/python-3.11-blue)](https://www.python.org/)
[![Conda Environment](https://img.shields.io/badge/conda-env-green)](https://docs.conda.io/)
[![Build Status](https://github.com/fabien-campillo/data-science-spikes/actions/workflows/build.yml/badge.svg)](https://github.com/fabien-campillo/data-science-spikes/actions)



## Installation

To install and run a Jupyter Book (or any Jupyter Notebook project), it is recommended to use a dedicated `conda` virtual environment defined in an `environment.yml` file.

To install and run a Jupyter Book (or any Jupyter Notebook project), it is recommended to use a dedicated `conda` virtual environment defined in an `environment.yml` file:

• create the virtual environment from the file `environment.yml`:

```bash
conda env create -f environment.yml
```

•  and activate the environment:

```bash
conda activate python-dsspikes-env
```
(`python-dsspikes-env` is the name of the virtual environment)

• if the virtual environment already exists and you want to update it:

```bash
conda env update -f environment.yml --prune
```
(`--prune` removes dependencies no longer listed in the YAML.)


`environment.yml ` is the YAML specification file of the Conda environment.

<br>


## Current `environment.yml`

```yml
name: python-dsspikes-env
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy=1.26  # compatible version for macOS
  - matplotlib=3.8
  - pandoc=3.8=h694c41f_0
  - seaborn=0.12
  - jupyter-book=1.0.4
  - jupyterlab=4.1
  - ipykernel
  - pip
  - pip:
      - pyabf==2.3.8  # install via pip
```
all installed with `conda` ecxept `pylab` only available with `pip`.


## Directory tree



```
(python-dsspikes-env) 0-git.nosync$ tree data-science-spikes/
data-science-spikes/
├── _build                       # generated by Jupyter Book (JB)
├── _config.yml                  # _config of the JB
├── _toc.yml                     # _toc of the JB
├── 1_context.ipynb              # chapters of the JB
├── 2_resources.ipynb            #    ...
├── 3_databases.ipynb            #    ...
├── 4_pyabf.ipynb                #    ...   
├── A_installing_python.ipynb    #    ...
├── B_jupyter.ipynb              #    ...
├── C_diagnostic.ipynb           #    ...
├── data                         # data used by the JB
├── dev                          # development dir
├── environment.yml              # conda virtual env definition
├── guide_jupyter_book_git.md    # hints 
├── pdf                          # pdf objects used for generating the  PDF
├── pics                         # somes pics
├── README.md                    # this file
├── utils                        # some python scripts
├── Z_references.ipynb           # references (in devo)
└── Z_sandbox.ipynb              # a sandbox chapter
```

## Author



[Fabien Campillo](https://www-sop.inria.fr/members/Fabien.Campillo/index.html)


